package com.aiticket.ticket.dto;

import lombok.Data;

@Data
public class AcceptTicketRequest {
    // Currently empty - handler just clicks "accept"
    // Future: could add custom price negotiation
}
